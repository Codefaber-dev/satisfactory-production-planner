# SPEC — Game Data Update v1.2

## §G — Goal

Backport `app/Helpers/UpdateOneZero.php` into the canonical seeders so a fresh `db:seed` produces an identical game-data state as the live migration. Also fix v1.1 recipe rate errors (Iron/Copper Alloy Ingots) that are absent from the helper, rename two legacy ingredient names (`SAM Ore` → `SAM`, `Green Power Slug` → `Blue Power Slug`), and add Ficsonium Fuel Rod support to the power planner.

## §C — Constraints

- `UpdateOneZero.php` is the authoritative source for all v1.0+ data; seeders must reproduce its final state on a fresh install
- RecipeSeeder format: `'Building' => ['Product' => recipe_data]`; byproducts use separate `byproducts` key; ingredients stored as per-minute consumption rates; use plain string names (not Enum::value) to match existing seeder style
- IngredientSeeder currently uses `'SAM Ore'` (tier1) and `'Green Power Slug'` (tier1); both names must change to canonical values: `'SAM'` and `'Blue Power Slug'`; any downstream seeder rows referencing the old names must also update
- BuildingSeeder currently has mk1–mk4 for each building; Converter and Quantum Encoder add mk1-only (base_power 500 each, matching helper)
- Excited Photonic Matter has zero material inputs — seeder uses empty `ingredients` array `[]`; production calculator must not throw on zero-input recipe
- SAM (raw) extraction uses Mk.2 miner (15 MW, 120/min) → 7.5 MJ/item; no rarity entry (inexhaustible nodes)
- Iron Alloy Ingot v1.1 change NOT in helper: output 50→75/min, Iron Ore 20→40/min, Copper Ore 20→10/min
- Copper Alloy Ingot v1.1 change NOT in helper: Iron Ore 25→50/min input; output/Copper Ore unchanged
- Quantum Encoder AI Expansion Server: Dark Matter Residue is byproduct (100/min), not input
- Ficsonium Fuel Rod: Nuclear Power Plant, 1/min, 2500 MW, zero waste — no waste product key
- Extraction data (satisfactory.wiki.gg): Miner Mk.1/Mk.2/Mk.3 = 60/120/240 items/min @100% on a normal node; power 5/15/45 MW. Purity multiplier: impure ×0.5, normal ×1, pure ×2. Overclock ≤250% via power shards (+50% each, max 3 → 100/150/200/250%); power scales `(clock/100)^1.321928` (energy model, V15).
- Water Extractor: 120/min @100%, 20 MW; placed on deep water, NOT node-bound → no purity/tier, quantity = extractor count (water exception per user req).
- Oil Extractor: impure/normal/pure = 60/120/240 /min @100%, 40 MW; purity applies, single building (no tier).
- Resource Well Pressurizer (Nitrogen Gas; well water/oil variants): 150 MW; feeds multiple satellite nodes each w/ own purity — modeled as a single **aggregate canonical rate** per wiki (satellite nodes NOT individually configured; one rate + purity for the well as a whole — decision locked).
- Converter ore-conversion recipes already seeded (UpdateOneZero Converter block, T68): raw ore produced from another raw + Reanimated SAM — these are the "convert" source option for raw ingredients (V61).
- AWESOME Sink points (satisfactory.wiki.gg): each item has a sink point value; recyclable points/min = qty/min × points. Only **solid** items are sinkable — unpackaged fluids/gases (`is_liquid` true) are NOT sinkable (their Packaged forms are sinkable items); a few items have no sink value → 0, not recycled. No points data currently in DB → new `sink_points` column needed (T108).
- Unpackage recipes seeded (Packager) for all 12 fluids/gases that have a packaged form (Recipe enum `UNPACKAGE_*` / Ingredient enum `PACKAGED_*`): Alumina Solution, Fuel, Heavy Oil Residue, Ionized Fuel, Liquid Biofuel, Nitric Acid, Nitrogen Gas, Crude Oil (Oil), Rocket Fuel, Sulfuric Acid, Turbofuel, Water. Each consumes the Packaged form, returns the empty container (Empty Canister / Empty Fluid Tank) as byproduct. Of these only Water/Crude Oil/Nitrogen Gas are raw (tier-1); the other 9 are produced intermediates. These back the "unpackage" option (V63).

## §I — Surfaces

- `I.helper-six` — `app/Helpers/UpdateSix.php` (Update 6 combat/weapon content)
- `I.helper-1.0` — `app/Helpers/UpdateOneZero.php` (v1.0 endgame content: SAM chain, Converter, QE)
- `I.helper-1.0b` — `app/Helpers/UpdateOneZero2.php` (v1.0 patches: rate fixes, more alts, cleanup)
- `I.ingredient-seeder` — `database/seeders/IngredientSeeder.php`
- `I.recipe-seeder` — `database/seeders/RecipeSeeder.php`
- `I.building-seeder` — `database/seeders/BuildingSeeder.php`
- `I.raw-materials` — `config/raw_materials.php`
- `I.power-planner` — `app/PowerPlanner/` (FicsoniumFuelRod generator class)
- `I.tests` — `tests/` (RecipeEnergyTest, seeder smoke tests)
- `I.plan-params` — per-plan params object passed to `ProductionCalculator` (clock_speed, somersloop_slots, recipe choices, etc.)
- `I.build-diagram` — `resources/js/Pages/Production/BuildDiagram.vue` (footprint render: foundations grid, building tiles, monogram)
- `I.building-settings` — Building Settings panel in `resources/js/Pages/Production/Show.vue` (per-type count-multiple inputs)
- `I.local-storage` — browser localStorage keys (blueprint sizes global, per-factory toggles)
- `I.fill-output` — per-output "Fill to 100%" action on each requested output's production step (`ProductionStep.vue`), un-gated for finals (V55)
- `I.perfect-ratio` — "Perfect Ratio" plan-level action in Plan Settings panel (`Show.vue`) — scales plan to smallest all-buildings-whole-at-100% size (V56)
- `I.loop-solver` — circular-dependency resolution in the production calculator (`app/Production/`): precomputed candidate-loop catalog (cached Tarjan SCC over full recipe catalog) + per-plan active-loop confirmation + linear balance-matrix solve, replacing forced-recipe swap (V58); follow-ups: node-split conflation detection (V68) + sourceless-loop source injection (V69) to retire the forced override
- `I.raw-step` — raw-ingredient production step + per-raw source-mode controls (import | extract | convert) with extraction params (node purity, miner tier, power shards) in ProductionStep/Show UI; tier-0 extractor diagram render; `raw_sources` plan param (V59–V62)
- `I.recycling` — AWESOME Sink recycling: per-item `sink_points` data, plan-level points/min output, auto-recycle of unused byproduct, auto-package-and-recycle-excess-fluids toggle in Building Settings (V65–V67)

## §V — Invariants

- **V21** After seeding: `Ingredient::ofName('SAM')` exists (not `'SAM Ore'`); `Ingredient::ofName('Blue Power Slug')` exists (not `'Green Power Slug'`); `Ingredient::ofName()` returns model for every item in `UpdateOneZero::ingredients()` plus `'Ficsonium Fuel Rod'`.
- **V22** Iron Alloy Ingot recipe in DB: `base_per_min=75`, Iron Ore 40/min, Copper Ore 10/min, building = Foundry.
- **V23** Copper Alloy Ingot recipe in DB: `base_per_min=100`, Copper Ore 50/min, Iron Ore 50/min, building = Foundry.
- **V24** `Building::ofName('Converter')` and `Building::ofName('Quantum Encoder')` both return model with `mk1` variant (base_power 500).
- **V25** All Converter recipes seeded (from both helpers); Excited Photonic Matter has empty ingredients array; Dark Matter Residue uses 50 Reanimated SAM/min (UpdateOneZero2 corrected rate, not 5).
- **V26** All 6 `UpdateOneZero::recipes()['Quantum Encoder']` entries seeded; Dark Matter Residue is byproduct (not input) at 1:1 with Excited Photonic Matter.
- **V27** All Diamond recipes (5) and Ficsonium recipe from `UpdateOneZero::recipes()['Particle Accelerator']` seeded; Dark Matter Crystal recipes (3) seeded.
- **V28** `php artisan db:seed` completes without exceptions.
- **V29** `RecipeEnergyTest` passes without regression after V22/V23 corrections.
- **V30** `NuclearPowerPlant` fuel map includes `Ingredient::FICSONIUM_FUEL_ROD` at `fuel_per_min=1`; no standalone `FicsoniumFuelRod` generator class exists.
- **V31** `FuelGenerator` fuel map has no duplicate keys; includes all 5 valid liquid fuels: Fuel (750 MJ), Turbofuel (2000 MJ), Liquid Biofuel (750 MJ), Rocket Fuel (3600 MJ), Ionized Fuel (5000 MJ). Packaged variants excluded (Fuel Generator is pipe-input only). `Base::buildable_fuels` includes `ROCKET_FUEL`, `IONIZED_FUEL`, `FICSONIUM_FUEL_ROD`.
- **V32** Factory PATCH (`PATCH /factories/:id`) persists full recipe-choice state: re-loading the factory via its Plan link restores identical recipe selections (same recipe ids, same nested sub-choices) as when saved. No recipe reverts to default on reload.
- **V33** Clicking Update on a multi-output plan with N outputs that each have a valid `product` and `recipe` returns a plan containing those same N outputs. No valid output is silently dropped after the Inertia round-trip.
- **V34** Somersloop slots stored per building type: Smelter=1, Constructor=1, Foundry=2, Assembler=2, Refinery=2, Blender=4, Particle Accelerator=4, Packager=0. Each filled slot adds proportional output boost up to 2× at max slots. Power consumption scales up to 4× at max amplification. UI allows per-building-instance slot selection (0 to max_slots) on each production step.
- **V35** `php artisan satis:export-game-data` (or equivalent) produces a portable JSON/SQL dump of all game-data tables (ingredients, recipes, buildings, raw_materials) suitable for seeding a fresh Docker install without running all migration helpers.
- **V36** Recipe cost multiplier is a numeric plan param (default 1.0, range 0.1–10.0). When set, all ingredient consumption quantities in `BuildingDetails` are multiplied by it (output qty and building count unchanged). Setting persists in plan params across fetch/save. UI exposes it as a numeric input (or preset buttons) alongside clock_speed controls. **UI placement superseded by V51.**
- **V37** Power cost multiplier is a numeric plan param (default 1.0, range 0.1–10.0, step 0.1). When set, all building power consumption values in the plan output are multiplied by it (ingredient qty and building count unchanged). Persists in plan params. UI exposes it alongside other plan-level power controls. **UI placement superseded by V51.**
- **V38** UI renders usably on mobile viewports (≥320px): no horizontal overflow on any plan page; recipe selectors, building steps, and output controls are legible and tappable (min 44px touch targets) at 375px width.
- **V39** Per-building-type count-multiple: each building type can have a user-configured step value (positive integer, default 1). Building count in plan output rounds up to nearest multiple of that step. Config persists in plan params. UI allows setting per-type multiples in a building settings panel.
- **V40** ∀ backend lookup keyed by product+recipe into request params (`somersloops`, future per-step params): key = `name|(recipe->description ?? name)` — frontend convention (`recipe.description || product.name`, ProductionStep.vue). `getDescription()` 'default' fallback ⊥ forbidden in request-param keys. Single source: `Step::getProductKey()`. Test must use frontend-shaped key (`Iron Ingot|Iron Ingot`, not `Iron Ingot|default`).
- **V41** Blueprint grouping toggleable per building type per factory. Blueprint size X = the V39 count-multiple (same number, one param — not a second value). Toggle ON for a type: building count rounds up to multiple of X (V39 mechanism active with multiple=X), build diagram renders count/X blueprint tiles instead of individual building outlines, each tile labeled `<monogram>x<X>` (e.g. `Cx8`, `Sx10`). Toggle OFF: effective multiple = 1, diagram renders individual building tiles unchanged from current behavior.
- **V42** Persistence split: blueprint sizes per building type → global localStorage (shared across all factories — blueprints reusable in-game); per-type enable toggles → localStorage keyed by factory id. Backend contract unchanged: effective multiples (size if enabled, else 1) still sent via `building_multiples` plan param; no server schema change. Fresh browser/no LS = all toggles off, all sizes 1.
- **V43** Blueprint designer mark selectable: Mk.1 = 32×32 m (4×4 foundations), Mk.2 = 40×40 m (5×5), Mk.3 = 48×48 m (6×6). Grouped diagram tile drawn at selected designer footprint dims. No fit validation/warning — in-game workarounds exist (clipping, vertical stacking). Blueprint size hard-clamped to 1–40 buildings per group (input max + clamp on LS read).
- **V44** Grouping supersedes even-rows grid-fill: when effective multiple X > 1 for a building type, the grid-fill adjustment (`num_buildings = rows × buildings_per_row`, from `even` param or `building_delta > 1` auto-trigger) must not fire; count stays exact multiple of X. Auto-trigger applies only to ungrouped types; explicit `even` under grouping → V46.
- **V45** Grouped diagram respects belt rates: grouped row count = min(tiles, max(near-square rows, backend `footprint.rows`)) — backend rows already encode belt_speed + speedLimit, so per-row belt load ≤ belt_speed whenever tiles ≥ backend rows. When tiles < backend rows (single blueprint exceeds one belt), layout caps at one tile per row and the >100% utilization highlight flags it — never silently under-rowed.
- **V46** Explicit `even` + grouping forces even stamp count: when multiple X > 1 and `even` plan param set, stamp count (`num_buildings / X`) rounds up to nearest even number — `num_buildings` stays exact multiple of X (V44 holds); clock_speed, shards, power_usage, build_cost, energy_per_item, total_energy recomputed against bumped count (V15 formulas). Already-even stamp count unchanged. `building_delta` auto-trigger never bumps stamps. **Superseded by V50** — parity semantics misread user intent (B37).
- **V47** V46 stamp bump preserves belt-derived rows: `footprint.rows` = belt-required row count (belt_load/qty vs belt_speed + speedLimit) identical before/after bump — bump changes num_buildings + clock_speed only, total throughput constant, per-row belt load unchanged; `buildings_per_row` recomputed against unchanged rows. Honors V45 frontend contract (backend rows = belt minimum) ∧ rows stays int.
- **V48** Building cost multiplier is a numeric plan param `building_cost_multiplier` (float, default 1.0, range 0.1–10.0). When set, all `build_cost` ingredient quantities in `BuildingDetails` (both normal path + adjusted-count recompute block) are multiplied by it. Strictly disjoint from `cost_multiplier` (V36): `cost_multiplier` scales recipe ingredient consumption only, never `build_cost`; `building_cost_multiplier` scales `build_cost` only — never ingredient consumption, belt load, power, or building counts. Persists in plan params across fetch/save. UI control alongside cost_multiplier. **UI placement superseded by V51.**
- **V49** Force-even under grouping yields uniform stamp rows: when applied `even` plan param set ∧ type grouped, grouped diagram row count = divisor of tile count (every row same stamp count, no ragged last row), chosen closest to near-square target, subject to rows ≥ belt-required rows (V45 floor, capped at tile count); tie → fewer rows. Layout consumes the applied (server round-trip) `even` value, not the pending toggle (T88 snapshot pattern). `even` off → near-square layout unchanged.
- **V50** Force-even + grouping = stamp grid-fill (grouped analog of ungrouped even-rows, supersedes V46 parity): `layout_rows = min(stamps, max(ceil(stamps/ceil(√stamps)), belt rows))`; stamp count rounds up to `layout_rows × ceil(stamps/layout_rows)` — no ragged last stamp row. Full grid already (e.g. 9 stamps at 3×3) → no-op, count unchanged. Stamp parity ⊥ goal. `num_buildings` stays exact multiple of X (V44); rows stays belt-derived (V47); clock/shards/power/cost/energy recomputed against bumped count (V15).
- **V51** Plan-level settings controls (belt_speed select, cost_multiplier, building_cost_multiplier, power_multiplier inputs) live in Building Settings panel (I.building-settings), not header output row — supersedes UI-placement clauses of V36/V37/V48 (param semantics, ranges, persistence unchanged). Settings button + panel reachable pre-fetch (plan-level section ⊥ `uniqueBuildings` gate); blueprint per-type rows still gated on uniqueBuildings. Bindings, fetch/save payload, multiplier prop flow unchanged — values apply on Update as before.
- **V52** Each plan-level setting (belt_speed, cost_multiplier, building_cost_multiplier, power_multiplier) renders as own card in Plan Settings panel: game-asset icon (`<img src="/images/<X>.png">`, local public dir — not Cloudinary; ConveyorBeltMk5 + Constructor downloaded from wiki, FicsitCoupon + PowerShard pre-existing) + text label + control. Cards flex-wrap (`flex-1` + min-width basis) — stack/reflow at 375px, no horizontal overflow (V38). V51 bindings/payload unchanged. **Icon clause superseded by V53** (Heroicon components replace the png `<img>`s).
- **V53** Plan Settings card icons are Heroicon outline components (`@heroicons/vue/24/outline`: `RocketLaunchIcon`=Belt Speed, `QueueListIcon`=Recipe Cost, `BuildingOffice2Icon`=Building Cost, `PowerIcon`=Power Cost), registered in Show.vue `components` — not `<img src="/images/*.png">` (supersedes V52 icon clause; png assets no longer referenced by these cards). belt_speed select option labels show throughput+tier (`<N> per min (mk<k>)`, e.g. `1200 per min (mk6)`), not bare `Belts mk<k>`. Card label styling text-xl/font-bold. V51 bindings/payload unchanged. Adds `@heroicons/vue` devDependency.
- **V54** ShowProduction tests track the V53 icon swap: the T92/V52 card test asserts each card renders its expected Heroicon via `findComponent(<Icon>)` (not `img[src=...png]`); belt-select matcher keys on option text `1200 per min (mk6)` (not `Belts mk6`); 4-cards/label/control assertions otherwise unchanged. AppLayout stub in `makeHeaderWrapper` must use the named slot `<slot name="header" />` so the `#header` content (Plan Settings panel) renders. Footgun: renaming to `<slot id="header" />` (e.g. to silence an IDE "name not a recognized attribute" warning on the inline template string) drops the content to the default slot — panel never mounts, all 4 T91 header tests fail. Suppress the IDE inspection instead; do not change the attribute.
- **V55** Per-output Fill to 100% (loop-aware; supersedes B31 won't-fix): action on a requested/final output (un-gates the maximize action that `v-show="!material.outputs?.final"` currently hides for finals). Sets *that one* output's yield to its 100%-clock fill value (`base_per_min × num_buildings × max_clock_speed / 100` = existing `maximizeOutput` newQty) and refetches — all other outputs' yields untouched. Distinct from Scale Up (global scalar on every yield, ScaleOutputs) and Output Extra (adds a separate output line). Result: selected product's own buildings → integer count @100%; mutually-dependent products keep their **net** yield fixed but their building counts may stay fractional (loop coupling — others' net outputs ⊥ changed). Selected product's machine count is linear in its own net yield when others held fixed, so `ratio = ceil(machines)/machines`. Plain yield edit; persists via normal save/fetch.
- **V59** Each raw ingredient is a selectable production step with a per-raw source mode (plan param `raw_sources`, map raw→config). Modes: `import` (default — current leaf behavior, raw is a tree leaf, contributes only its proxy energy cost per `config/raw_materials.php`, no extractor/building), `extract` (V60), `convert` (V61), `unpackage` (V63, fluid/gas raws only). Default = `import` for every raw → plan output byte-identical to current. Persists in plan params across fetch/save.
- **V60** Extract mode = a tier-0 extraction step. Per-extractor rate = `base_rate × purity × overclock`, where overclock = `(100 + 50·shards)/100` (shards 0–3 → ≤250%). Extractors/nodes needed = `ceil(demand / per-extractor-rate)`. Power = `count × extractor_power × (clock/100)^1.321928` (V15). Extractor type + params dispatched by raw: **Miner** (solid ores + SAM; params purity + miner tier mk1/2/3 + shards; rates/power §C), **Water Extractor** (Water; shards only — no purity/tier, unlimited placement, quantity = extractor count), **Oil Extractor** (Crude Oil; purity + shards, no tier), **Resource Well Pressurizer** (Nitrogen Gas + well variants; purity + shards, aggregate rate §C). Replaces the raw's import proxy-energy with real extractor power in the plan's power total.
- **V61** Convert mode = the raw is treated as a processed step using a user-selected Converter ore-conversion recipe (seeded, T68 — raw produced from another raw + Reanimated SAM). It recurses into the converter recipe's own raw inputs, each carrying its own `raw_sources` mode. Convert chains may form raw↔raw loops (e.g. Iron via Coal via Iron) → resolved by the V58 linear loop solver, never by forcing a recipe.
- **V63** Unpackage coverage = ALL 12 fluids/gases that have a packaged form (§C list). Two paths by kind, so nothing is missed: **(a) raw** (Water, Crude Oil, Nitrogen Gas) → unpackage is a `raw_sources` mode (V59): the raw is produced via its Unpackage recipe (Packager), Packaged input child defaults to `import` ("import the packaged contents"); shares the raw-via-recipe path with convert (V61). **(b) non-raw intermediates** (the other 9 — Alumina Solution, Fuel, Heavy Oil Residue, Ionized Fuel, Liquid Biofuel, Nitric Acid, Rocket Fuel, Sulfuric Acid, Turbofuel) → unpackage = selecting the intermediate's Unpackage recipe via normal recipe choice (T106); its Packaged input is then imported (existing imports mechanism) or produced. Both paths: Unpackage consumes the Packaged form, returns the empty container (Empty Canister / Empty Fluid Tank) byproduct; the X ↔ Packaged X loop is resolved by the V58 solver, never forced. Every packaged fluid/gas is reachable by exactly one path — coverage complete.
- **V62** Extract-step diagram (tier-0): renders a single extractor tile with a `×N` multiplier badge, N = node/extractor count from V60 (e.g. one Miner Mk.2 tile ×3 nodes). Water renders `×N` = extractor count (no node/purity semantics). Honors build-diagram conventions (I.build-diagram).
- **V64** Import note: each imported ingredient (raw `import` mode V59, or any ingredient via the imports mechanism) accepts an optional free-text note (e.g. "From NW Steel Factory"), displayed on that ingredient's production step. Stored in `import_notes` plan param (map ingredient→note), persisted to the saved factory (single + multi) and restored on reload (V32/V57 round-trip). Empty note = no display; import calc behavior otherwise unchanged.
- **V65** AWESOME Sink points data: each sinkable ingredient has a `sink_points` value (int, sourced satisfactory.wiki.gg §C). Sinkability = solid only: unpackaged fluids/gases (`is_liquid` true) NOT sinkable (their Packaged forms are sinkable items); no/zero sink value → not recycled. Recyclable points/min for any flow = qty/min × sink_points.
- **V66** Recycling output + auto-recycle unused byproduct: plan surfaces a total recycled **points/min** figure as an output. Unused byproduct (produced − consumed = Multiplexer `getByproducts` − `getByproductsUsed`) of sinkable items (V65) is auto-recycled → its points/min added to the total. Non-sinkable leftover (fluids) is recycled only when the auto-package toggle is on (V67) — packaged then sunk; otherwise surfaced as waste. Total points/min = Σ recycled flows × sink_points.
- **V67** Auto-package-and-recycle excess fluids: a Building Settings toggle (plan param `auto_package_recycle`, default OFF). When ON, unused fluid/gas byproduct (non-sinkable as liquid, V65) is routed through its Package recipe (Packager → Packaged form, which IS sinkable) then auto-recycled → points/min (V66); the added Packager step's building count + power + empty-container input (Empty Canister / Empty Fluid Tank) are included in the plan. Only fluids that have a Package recipe with a sinkable packaged form qualify; others stay surfaced as waste. OFF = current behavior (fluid leftover shown as waste, never packaged). Persists in plan params + saved factory.
- **V68** Loop conflation resolved by node-splitting, replacing the byproduct-boundary heuristic (B43, T100b Fix 1). When the same product plays distinct roles — supplied by a recipe AND by a byproduct, or otherwise multi-sourced — split it into distinct nodes (`<product>@<source>`) in the loop graph so a spurious SCC can't form (caterium's Empty Canister: `@byproduct` vs `@recipe`). Replaces "byproduct-supplied → boundary" (which blunt-drops the product entirely) with principled differentiation; the real loops still detect/solve, the byproduct supply is modeled where it actually flows. Acyclic + solved-loop outputs unchanged (canary).
- **V69** Sourceless-loop source injection, replacing `overrideFavoritesIfNecessary` (B44) + the generalized forced swap. When an active loop is unsolvable (`det=0`/non-productive, V58) AND sourceless (no member fed from outside the SCC — not imported, no boundary feed), inject a source instead of hardcoded forcing: **(a) recipe-swap** — switch one member to its most-efficient alternate recipe whose ingredients don't re-enter the loop (general, efficiency-ranked — supersedes `useCompatibleRecipe`'s `->first()`). Member pick order: **preserve an unpackage choice** — choosing `Unpackage X` deliberately asserts the packaged form comes from elsewhere, so re-source the PACKAGED member (e.g. Packaged Fuel → Diluted Packaged Fuel) and keep `Unpackage X`; then prefer non-user-chosen members; then cheapest by `resource`. **(b) auto-import** — if no loop-free alternate exists, mark a member imported (supply externally). Surfaced as an informational note (NOT the "circular dependencies" alarm); user-overridable. A recipe the user *explicitly* chose is not silently swapped — prefer auto-import, or surface "no source — import or re-pick". Removes `overrideFavoritesIfNecessary` (Setters) + `useCompatibleRecipe` (CalculatesSteps) once both V68 + V69 land.
- **V58** Circular recipe dependencies resolved by linear solve, not by forcing a substitute recipe. **Two-stage detection.** Stage 1 (static, precomputed): build the directed graph over the *full* recipe catalog (incl. alts, edge product→ingredient per recipe), run Tarjan SCC → candidate loop clusters, each tagged with the `(product, recipe)` picks that enable its back-edges; cache it, invalidated on Recipe/Ingredient save (same hook as T35 — B17). Stage 2 (per plan): build the **selected-recipe subgraph** (one effective recipe per product = choice > favorite > base) and run Tarjan SCC on *that* — its multi-node SCCs are the active loops. The stage-1 catalog is used only as a cheap participation gate (skip the per-plan SCC entirely when the product set touches no candidate loop). The earlier "active iff selection covers the candidate cluster's enabling set" approach is abandoned (B41: full-catalog SCCs merge unrelated package/unpackage/recycled fluid loops into one giant blob whose enabling set is never fully selected). No dynamic mid-walk cycle-catching, no recursion-depth probing. For each active SCC (e.g. Recycled Plastic ⇄ Recycled Rubber) build the square balance matrix A (per-product net = recipe outputs − inputs across the SCC's selected recipes) and solve `A·x = demand` exactly (RREF/matrix inverse) for recipe run-rates x — **keeping the user's selected recipes**. Detection + solve must honor the same raw/import boundaries as the calc (an imported node cuts the loop). Acyclic remainder still resolved by the topological walk (current recursive path = back-substitution for a DAG). Removes the forced-recipe swap (`useCompatibleRecipe`, CalculatesSteps.php:85) and the hard-coded name overrides (`overrideFavoritesIfNecessary`, Setters.php:132 — Recycled Rubber/Plastic, Packaged Fuel). Warn + fall back only when a loop is genuinely unsolvable: `det(A)=0` / non-productive (no net output, negative gross) — never silently swap a valid loop. Same net/gross math as V55/V56. ProductionTree TopSort cut-as-import (ProductionTree.php:72) may remain for build-ordering only; quantities authoritative from the solver.
- **V57** Issue #4 restore guarantee (strengthens V32; the half T73 left untested): reloading a saved factory's Plan URL reconstructs *identical* recipe selections — proven by a round-trip test that drives the controller (`/dashboard/...?choices[...]` → `ProductionController::show`/`multi` + `baseData`) and asserts the re-derived selection state, NOT just the save payload (T73's test covered only the latter — cf B32). Choice capture (`allChosenRecipes`, Show.vue) must include the effective sub-recipe selections actually in force in the production tree, not merely `newChoices` + top-level outputs. Restore resolution must not silently collapse a stored non-default pick to its base recipe: `Recipe::ofName` returning a base-recipe fallback for an unmatched description, then `baseData` (ProductionController:29) default-rejecting it, must never revert a user choice.
- **V56** Perfect Ratio Factory mode: plan-level action scaling all output yields by smallest factor S s.t. every recipe step's building count is a whole integer at 100% clock (no fractional/underclocked machine anywhere). `S = LCM(denominators of all steps' exact fractional machine demand)` (rational machine counts). Reuses ScaleOutputs (one-shot scalar on every output yield, output ratios preserved); mutual loops covered — integer machine-vector falls out of the joint LCM. Needs exact fractional machine demand per step (expose from backend if the displayed count is already rounded). Guard: if S — or any resulting building count — exceeds cap → warn + do not apply (offer nearest feasible); factory could be impractically large. Not a persistent param — recompute on demand like Scale Up.

## §T — Tasks

| id  | status | task                                                                                      | cites          |
|-----|--------|-------------------------------------------------------------------------------------------|----------------|
| T48 | x      | Fix Iron Alloy Ingot in RecipeSeeder: base_yield 5→15, base_per_min 50→75, Iron Ore 20→40/min, Copper Ore 20→10/min (in UpdateOneZero2 Foundry block) | I.recipe-seeder, I.helper-1.0b, V22 |
| T49 | x      | Fix Copper Alloy Ingot in RecipeSeeder: base_yield 20→10, Iron Ore 25→50/min (in UpdateOneZero2 Foundry block) | I.recipe-seeder, I.helper-1.0b, V23 |
| T50 | x      | Remove deprecated items from IngredientSeeder: `Alien Carapace` (tier1), `Color Cartridge` (tier2), `Spiked Rebar` (tier4), `Beacon` (tier5), `Rifle Cartridge` (tier6) | I.ingredient-seeder, I.helper-six, I.helper-1.0b |
| T51 | x      | Remove deprecated recipes from RecipeSeeder: `Beacon`, `Color Cartridge`, `Steel Coated Plate`; also remove `Rifle Cartridge` recipe (uses deleted Beacon ingredient) | I.recipe-seeder, I.helper-1.0b |
| T52 | x      | Rename `'SAM Ore'`→`'SAM'` and `'Green Power Slug'`→`'Blue Power Slug'` in IngredientSeeder tier1; update all RecipeSeeder string references to match | I.ingredient-seeder, I.recipe-seeder, I.helper-1.0, V21 |
| T53 | x      | Add UpdateSix ingredients to IngredientSeeder: tier1 raw: Hog Remains, Plasma Spitter Remains, Hatcher Remains, Stinger Remains; tier2: Alien Protein, Organic Data Capsule, Gas Nobelisk, Pulse Nobelisk, Smokeless Powder; tier3: Nuke Nobelisk; tier4: Cluster Nobelisk, Explosive Rebar, Iron Rebar, Stun Rebar, Shatter Rebar; tier6: Rifle Ammo, Turbo Rifle Ammo, Homing Rifle Ammo | I.ingredient-seeder, I.helper-six, V21 |
| T54 | x      | Backport UpdateOneZero ingredients to IngredientSeeder using helper tier values; add Ficsonium Fuel Rod; use plain strings matching Ingredient enum values | I.ingredient-seeder, I.helper-1.0, V21 |
| T55 | x      | Add `'SAM'` energy cost to `config/raw_materials.php`: `'SAM' => 15 * 60 / 120` (7.5 MJ/item, Mk.2 miner); no rarity entry | I.raw-materials |
| T56 | x      | Add Converter to BuildingSeeder (mk1 only): 2 in / 2 out, base_power 500, build cost from helper: 10 Fused Modular Frame, 25 Cooling System, 50 Radio Control Unit, 100 SAM Fluctuator | I.building-seeder, I.helper-1.0, V24 |
| T57 | x      | Add Quantum Encoder to BuildingSeeder (mk1 only): 4 in / 2 out, base_power 500, build cost from helper: 20 Turbo Motor, 20 Supercomputer, 50 Cooling System, 50 Time Crystal, 100 Ficsite Trigon | I.building-seeder, I.helper-1.0, V24 |
| T58 | x      | Backport UpdateSix Constructor recipes to RecipeSeeder: Alien Protein (Hog/Spitter/Hatcher/Stinger variants), Biomass (Alien Protein alt), Organic Data Capsule, Iron Rebar | I.recipe-seeder, I.helper-six |
| T59 | x      | Backport UpdateSix Assembler/Refinery/Manufacturer/Blender recipes to RecipeSeeder: Black Powder (new Assembler version), Nobelisk, Gas Nobelisk, Pulse Nobelisk, Cluster Nobelisk, Rifle Ammo, Stun Rebar, Shatter Rebar, Homing Rifle Ammo; Smokeless Powder + Polyester Fabric (Refinery); Explosive Rebar, Nuke Nobelisk, Turbo Rifle Ammo (Manufacturer); Turbo Rifle Ammo Turbofuel variant (Blender) | I.recipe-seeder, I.helper-six |
| T60 | x      | Replace/update existing RecipeSeeder entries per UpdateOneZero Constructor additions: Alien DNA Capsule, Aluminum Beam alt, Aluminum Rod alt, Iron Pipe alt, Power Shard 1/2/5 alts, Reanimated SAM, Ficsite Trigon | I.recipe-seeder, I.helper-1.0, V21 |
| T61 | x      | Update Assembler recipes in RecipeSeeder per UpdateOneZero: Plastic AI Limiter alt, Silicon Circuit Board alt, updated Computer rates, OC Supercomputer update, Encased Industrial Pipe alt, updated Magnetic Field Generator; also UpdateOneZero2 additions: Fine Black Powder alt, Fine Concrete alt, Rubber Concrete alt, Coated Iron Plate alt, Automated Miner alt, Adhered Iron Plate alt, Copper Rotor alt, Cheap Silica alt | I.recipe-seeder, I.helper-1.0, I.helper-1.0b |
| T62 | x      | Update Foundry recipes in RecipeSeeder per UpdateOneZero: Basic Iron Ingot alt, Fused Quartz Crystal alt, Molded Beam alt, Molded Steel Pipe alt, Steel Cast Plate alt, Tempered Caterium Ingot alt, Tempered Copper Ingot alt; UpdateOneZero2: Compacted Steel Ingot alt | I.recipe-seeder, I.helper-1.0, I.helper-1.0b |
| T63 | x      | Update Refinery recipes in RecipeSeeder per UpdateOneZero: Electrode Aluminum Scrap alt, Ionized Fuel, Leached Caterium/Copper/Iron alts, Quartz Purification alt | I.recipe-seeder, I.helper-1.0 |
| T64 | x      | Update Manufacturer recipes in RecipeSeeder: UpdateOneZero additions (updated Computer/Supercomputer/Heavy Modular Frame, Automated Speed Wiring alt, Ballistic Warp Drive, Iodine Infused Filter, Rigor Motor alt, Uranium Fuel Unit alt, SAM Fluctuator, Singularity Cell); UpdateOneZero2 additions (Cooling Device alt, updated Adaptive Control Unit/Radio Control Unit, Radio Connection Unit alt, Gas Filter, updated Explosive Rebar rate) | I.recipe-seeder, I.helper-1.0, I.helper-1.0b, V21 |
| T65 | x      | Backport Blender/Packager recipe additions from UpdateOneZero: Biochemical Sculptor, Distilled Silica alt, Rocket Fuel, Nitro Rocket Fuel alt; Packaged Ionized Fuel, Packaged Rocket Fuel, Unpackage Ionized Fuel, Unpackage Rocket Fuel | I.recipe-seeder, I.helper-1.0 |
| T66 | x      | Backport Particle Accelerator recipes from UpdateOneZero: Diamonds (5 variants), Dark Matter Crystal (3 variants), Ficsonium | I.recipe-seeder, I.helper-1.0, V27 |
| T67 | x      | Backport Quantum Encoder recipes from UpdateOneZero: all 6 recipes with Dark Matter Residue byproduct | I.recipe-seeder, I.helper-1.0, V26 |
| T68 | x      | Backport Converter recipes from both helpers: Excited Photonic Matter (empty ingredients), Dark Matter Residue (50 Reanimated SAM/min — use UpdateOneZero2 corrected rate), Time Crystal, Ficsite Ingot (3 variants), Pink Diamonds, ore-conversion alts (both Bauxite/Caterium/Coal/Copper/Iron/Limestone/Nitrogen/Quartz/Sulfur/Uranium alts), Dark Ion Fuel alt | I.recipe-seeder, I.helper-1.0, I.helper-1.0b, V25 |
| T69 | x      | Create `FicsoniumFuelRod` power planner class in `app/PowerPlanner/` mirroring existing fuel rod classes: power_output=2500 MW, fuel_per_min=1, no byproduct | I.power-planner, V30 |
| T70 | x      | Run `php artisan db:seed` (fresh DB) and verify V28; log failures as §B entries | V28 |
| T71 | x      | Verify/update RecipeEnergyTest after T48/T49 corrections; run full test suite; verify ≥ 80% coverage | I.tests, V29 |
| T72 | x      | Fix `FuelGenerator`: remove duplicate `IONIZED_FUEL` key (second entry → `ROCKET_FUEL` at 3600 MJ); add `ROCKET_FUEL`, `IONIZED_FUEL`, `FICSONIUM_FUEL_ROD` to `Base::buildable_fuels` | I.power-planner, V31 |
| T73 | x      | Fix `saveMyFactory` PATCH: capture full recipe-choice state (all `newChoices` + current sub-recipe choices from the production tree) before sending to `/factories/:id`; verify reload restores identical selections — issue #4. **PARTIAL: send-side only** — `allChosenRecipes` captures newChoices + top-level outputs but NOT tree sub-recipes; reload/restore never tested → issue #4 persists (B39); restore fix → T97 | V32, B39 |
| T74 | x      | Fix multi-output line-disappears bug: trace Inertia component lifecycle on `fetch()` call after second output added; ensure all outputs with valid product+recipe survive the round-trip and are present in re-initialized `data()` `outputs` array — resolves issue #3 | V33 |
| T75 | x      | ~~Per-output Scale Up~~ — B31 won't-fix **reversed by B38**; superseded by T95 (per-output fill) + T96 (perfect ratio) | B38, V55 |
| T76 | x      | Somersloop support: per-building-instance slot selector (0…max_slots) on each production step; max_slots from V34 table; amplified output = base_per_min × (1 + slots/max_slots); power cost scales up to 4× at max; slot state persists in plan params across fetch — resolves issue #2 remaining item | V34 |
| T77 | x      | Create `php artisan satis:export-game-data` command that dumps ingredients/recipes/buildings/raw_materials to JSON; add README note for Docker self-hosters to run after seed — resolves issue #9 | V35 |
| T78 | x      | Recipe cost multiplier: add `cost_multiplier` plan param (float, default 1.0); apply in `BuildingDetails::calc()` to ingredient pivot quantities; thread param through `ProductionCalculator` → `CalculatesSteps` → `BuildingOverview`; persist in plan params via existing save/fetch flow; add UI control | I.plan-params, V36 |
| T79 | x      | Power cost multiplier: add `power_multiplier` plan param (float, default 1.0, range 0.1–10.0 step 0.1); apply to all power consumption values at display layer in `BuildingOverview`/plan summary; persist in plan params; add UI control (slider or numeric input) alongside clock_speed/somersloop controls | I.plan-params, V37 |
| T80 | x      | Responsive design pass: audit all plan pages at 375px; fix horizontal overflow (tables → cards or scroll-x); ensure recipe selectors are touch-friendly; stack multi-column layouts to single column below `md` breakpoint; verify on real mobile viewport | V38 |
| T81 | x      | Update checklist feature for v1.2: add new buildings (Converter, Quantum Encoder), new tier-9 ingredients and recipes from §T60–T68 to any checklist/milestone tracker; remove deprecated items (Alien Carapace, Color Cartridge, Spiked Rebar, Beacon, Rifle Cartridge) from checklists | V21, V24 |
| T82 | x      | Per-building count multiples: add `building_multiples` plan param (map of building_name → step int, default empty = all 1); apply in building-count rounding in `BuildingDetails`; expose in a "Building Settings" panel in the UI with per-type numeric inputs; persist via existing save/fetch flow | I.plan-params, V39 |
| T83 | x      | Rework Building Settings panel (Show.vue): per-type row = blueprint size input + enable toggle; sizes read/write global LS key (e.g. `bp_sizes`); toggles read/write per-factory LS key (e.g. `bp_enabled:<factory_id>`); derive effective `building_multiples` plan param (enabled → size, else 1) on change + on mount; immutably update state | I.building-settings, I.local-storage, I.plan-params, V41, V42 |
| T84 | x      | Diagram grouping: when type grouping enabled, BuildDiagram renders count/X blueprint tiles at designer dims with `<monogram>x<X>` label; individual building outlines hidden; footprint stats (foundations/walls/offsets/rows) recomputed against blueprint tile dims; toggle off = current render path untouched | I.build-diagram, V41, V43 |
| T85 | x      | Designer mk selector (Mk.1/2/3, global LS, default Mk.1); clamp blueprint size to 1–40 (input min/max + clamp on LS read); no fit warning | I.building-settings, I.local-storage, V43 |
| T86 | x      | Tests: LS persistence round-trip (sizes global, toggles per-factory), effective-multiple derivation, grouped tile count/label, size clamp 1–40, even-rows superseded when multiple > 1; backend `building_multiples` rounding already covered by T82 tests | V41, V42, V43, V44 |
| T87 | x      | Backend: skip even-rows branch in `BuildingDetails::getBuildingDetails()` when `$multiple > 1` (both `even` param and `building_delta` auto-trigger paths); recompute rows/buildings_per_row against grouped count instead | I.plan-params, V44 |
| T88 | x      | Building Settings apply button: toggle/size edits persist to LS + local state only — no auto-fetch per input; Update button inside panel triggers fetch; diagrams regroup from last-applied multiples (snapshot taken on every fetch) so panel edits stay invisible until applied; designer mk stays instant (render-only) | I.building-settings, V41, V42 |
| T89 | x      | Backend: explicit `even` + multiple > 1 in `BuildingDetails::getBuildingDetails()` rounds stamp count up to even (num_buildings = next even multiple of X); reuse recompute block (clock/shards/power/cost/energy); auto-trigger path unchanged | I.plan-params, V44, V46 |
| T90 | x      | Building cost multiplier: add `building_cost_multiplier` plan param (float, default 1.0, clamp 0.1–10.0); thread through `ProductionCalculator` → `BuildingDetails::calc` (mirror `cost_multiplier` path via `ProductionGlobals`); apply to `build_cost` maps in both normal + recompute blocks; persist via existing save/fetch flow; UI numeric input beside cost_multiplier; tests: build_cost scales, ingredient consumption/belt load/power/counts unchanged, disjointness from cost_multiplier | I.plan-params, V48 |
| T91 | x      | Move belt_speed select + cost/building-cost/power multiplier inputs from header output row (Show.vue:73–119) into Building Settings panel as "Plan Settings" group above Blueprint Mode; un-gate Settings button + panel (always visible; blueprint per-type rows keep uniqueBuildings gate); keep v-models, fetch payload, ProductionSteps cost-multiplier prop unchanged | I.building-settings, V51 |
| T92 | x      | Plan Settings cards: one card per setting (icon + label + control), flex-wrap responsive at 375px; icons ConveyorBeltMk5.png + Constructor.png (wiki, new in public/images) + FicsitCoupon.png + PowerShard.png (existing); tests: 4 cards w/ icons render in panel, T91 binding tests still green | I.building-settings, V51, V52, V38 |
| T93 | x      | Plan Settings icons → Heroicon components (RocketLaunch/QueueList/BuildingOffice2/Power, `@heroicons/vue/24/outline`); belt select option labels → `<N> per min (mk<k>)`; card label text-xl/bold; add `@heroicons/vue` devDependency | I.building-settings, V53 |
| T94 | x      | Update ShowProduction tests for Heroicon swap: `findComponent(<Icon>)` not `img[src]`, belt matcher on `1200 per min (mk6)`; AppLayout stub uses named slot `<slot name="header" />` so #header panel renders | I.building-settings, V54 |
| T95 | x      | Per-output Fill to 100%: un-gate a "Fill to 100%" action on requested/final output steps (`v-show="!material.outputs?.final"` at ProductionStep.vue:277 hides it for finals today); on click set that output's yield = `maximizeOutput` newQty (100%-clock fill) WITHOUT ScaleOutputs/AddOutput — edit only the selected output, then refetch; tests: selected output's buildings integer @100%, other outputs' yields unchanged, mutual-loop case (plastic+rubber residue alts) fills plastic alone | V55, B38, I.fill-output |
| T101 | .      | `raw_sources` plan param: map raw → `{mode: import\|extract\|convert\|unpackage, purity, miner, shards, recipe}`, default `import` for all; thread through ProductionCalculator → ProductionGlobals → Step (raw branch in CalculatesSteps.php:33); persist via save/fetch; default keeps current output byte-identical | I.raw-step, I.plan-params, V59 |
| T102 | .      | **TDD.** Extraction calc (pure unit, no DB): `rate = base × purity × overclock`, `nodes = ceil(demand/rate)`, `power = nodes × base_power × (clock/100)^1.321928`; extractor-type dispatch (miner mk1/2/3+purity+shards / water shards-only / oil purity+shards / well). Tests FIRST: Mk.2 normal 100% = 120/min; pure +3 shards = 120×2×2.5; node count ceil; water has no purity/tier (count only); power exponent. Then implement | I.raw-step, V60 |
| T103 | .      | Raw-via-recipe modes (convert + unpackage, shared path): raw step uses a selected recipe — Converter ore-conversion (T68 seed) for `convert`, or Unpackage Water/Oil/Nitrogen Gas (Packager) for `unpackage` (returns empty-container byproduct; Packaged input child defaults to import); recurses into inputs (each own source mode); raw↔raw convert loops resolved via V58 solver. Tests: Coal→Iron convert consumes Iron Ore + Reanimated SAM; Iron⇄Coal mutual convert resolves via solver, not forced; Water unpackage consumes Packaged Water + emits Empty Canister | I.raw-step, V61, V63, V58 |
| T104 | .      | Frontend raw-step UI: each raw renders a step with source-mode selector (import/extract/convert/unpackage — unpackage only for fluid/gas raws); extract shows purity + miner-tier + power-shard controls (per extractor type — water hides purity/tier); convert shows Converter-recipe selector; unpackage shows the Unpackage recipe + its Packaged input (defaults import); bind to `raw_sources`, include in fetch payload; un-gate from current import-only display | I.raw-step, V59, V60, V61, V63 |
| T105 | .      | Extract diagram: tier-0 step renders single extractor tile + `×N` multiplier badge (node/extractor count, V62); water = `×N` extractor count, no purity. Tests: miner tile + multiplier matches node count, water variant | I.raw-step, I.build-diagram, V62 |
| T107 | .      | Import note: free-text input per imported ingredient (raw import mode + imports mechanism); display the note on that ingredient's production step; `import_notes` plan param (map ingredient→note); persist to saved factory (single + multi); restore on reload. Tests: note round-trips save→reload, shows on step, empty hidden | I.plan-params, V64, V32, V57 |
| T108 | .      | Sink points data: add `sink_points` column to ingredients (migration); seed values from wiki; sinkability via `is_liquid` (unpackaged fluids/gases not sinkable) + zero/unsinkable exclusions; cache-invalidate on save (T35 hook). Tests: known solid item has points, unpackaged fluid not sinkable, packaged fluid sinkable | I.recycling, V65 |
| T109 | .      | **TDD.** Recycling calc (pure unit): points/min = qty × sink_points; auto-recycle unused byproduct (produced − used, sinkable only); auto-package excess fluids when toggle on — route fluid leftover → Package recipe → Packaged (sinkable) → sink, adding Packager building/power/empty-container input; aggregate plan points/min. Tests FIRST: sinkable byproduct leftover → points, fluid leftover toggle-off → waste/0, fluid leftover toggle-on → packaged + points + Packager step, total aggregation. Then implement | I.recycling, V66, V67 |
| T110 | .      | Frontend recycling UI: plan-level points/min output line; per-step recycled-byproduct indicator; `auto_package_recycle` toggle in Building Settings panel (I.building-settings); bind + include in fetch + persist. Tests: points/min renders, toggle round-trips, packaged-fluid recycle reflected | I.recycling, I.building-settings, V66, V67 |
| T106 | .      | Non-raw fluid/gas unpackage path (the 9 intermediates, V63b): each intermediate w/ a packaged form exposes its Unpackage recipe as a selectable recipe choice; selecting it sources the Packaged input via import (or production); X↔Packaged X loop resolved by V58; remove hardcoded packaged-fuel override (Setters.php:150 — also in T100a). Tests: select Unpackage Turbofuel → Packaged Turbofuel importable input + Empty Canister byproduct; all 9 intermediates have selectable Unpackage recipe; loop resolves | V63, V58, I.raw-step |
| T98 | x      | **TDD.** Precomputed candidate-loop catalog: pure unit (no DB/HTTP) — Tarjan SCC over the full recipe-catalog graph (edge product→ingredient per recipe incl. alts), output = clusters each tagged with enabling `(product, recipe)` picks; cache + invalidate on Recipe/Ingredient save (T35 hook). Tests FIRST (RED): known loop pair (Recycled Plastic/Rubber) appears as a candidate cluster; acyclic-only catalog has no clusters; cache busts on recipe save; import/raw node excluded from edges. Then implement to green | V58, I.loop-solver |
| T99 | x      | **TDD.** Active-loop detection + linear solver: `LoopSolver::solve` builds square balance matrix A (net = outputs − inputs over the SCC's selected recipes), solves `A·x = demand` exactly for run-rates; unsolvable (`det(A)=0` / non-productive) → null. **Rework (B41):** active-loop detection must run Tarjan SCC on the selected-recipe subgraph (reuse `LoopCatalog::detect` with one effective recipe per product), NOT enabling-set cover on full-catalog clusters — the latter blobs unrelated fluid loops together. `LoopSolver::solve` + its tests stand; replace the `activeLoops` enabling-set path | V58, I.loop-solver, V55, B41 |
| T100 | x      | **SPLIT** → T100a + T100b (both done) (engine refactor too large/risky for one atomic step; tiering OK — static `product->tier` — but solved quantities must thread through the single-pass recursive walk, and 2 tests encode the old forced-recipe behavior). Parent tracker only; build T100a then T100b | V58, I.loop-solver |
| T100a | x      | Wire solver into ProductionCalculator — **root-loop case** (output product itself is in a loop): gate on `LoopCatalog::all()` (root participates in a candidate loop), then detect the active SCC by running `LoopCatalog::detect` on the selected-recipe subgraph (B41); if the root product ∈ that active SCC, `LoopSolver::solve` for gross (external demand = root qty on root, 0 on other members) and inject into the Step walk via globals (loop members use solved gross as qty; back-edges to loop members cut by chain-contains — no infinite recursion; `overrideFavoritesIfNecessary` skipped for the solved root-loop). All new behavior gated behind a populated `loopGross` → when root ∉ any active loop (incl. acyclic plans + deeper loops like Caterium Computer) behavior is byte-identical and the existing `useCompatibleRecipe`/`overrideFavoritesIfNecessary` hacks remain the fallback. Rewrite only `it_can_handle_circular_dependencies` (RecipeProductionTest) to solved numbers (both recycled recipes kept) | V58, V55, I.loop-solver, B40 |
| T100b | x      | **DONE (hybrid, B43).** Iterative-convergence loop solver: detect active loops as SCCs of the selected-recipe subgraph with byproduct-supplied products as boundary (Fix 1); accumulate external loop-member demand tree-wide; converge to steady state (handles nested loops); emit each member once at solved gross; keep the forced-recipe fallback (`useCompatibleRecipe`/`overrideFavoritesIfNecessary`) but skip it for solved-loop members and gate `activeLoops` to **solvable** loops only — degenerate/singular loops (1:1 Fuel⇄Packaged Fuel) fall back to forced recipe. caterium = hybrid (Plastic⇄Rubber solved, Packaged Fuel forced); root Plastic⇄Rubber solved; acyclic byte-identical (canary). Original design notes (B42, B43) below. Iterative-convergence solver was built + reverted (B43). Open requirements: (a) iterative-to-convergence solving for nested loops (caterium: Plastic⇄Rubber feeds Fuel⇄Packaged Fuel) — DONE in the reverted attempt; (b) **byproduct-aware loop detection** (B43) — Empty Canister is byproduct-supplied yet has a Plastic recipe, fusing the loops into one unsolvable SCC; must treat byproduct-supplied products as boundary or model byproduct supply in the balance matrix; (c) external-demand accumulation preserving per-consumer demand — DONE; then multi-output; remove `useCompatibleRecipe` (CalculatesSteps.php:85) + `overrideFavoritesIfNecessary` (Setters.php:132) keeping the universal back-edge cut + an unsolvable→forced fallback so complex loops don't regress; warn on unsolvable. Rewrite `it_can_resolve_a_circular_dependency` + caterium; Packaged Fuel coverage; validate vs satisfactory-calculator + rebaseline canary. Until then deeper loops stay on the forced-recipe fallback (canary-captured) | V58, I.loop-solver, B42, B43 |
| T97 | x      | Issue #4 restore fix + test: (1) extend `allChosenRecipes` (Show.vue:502) to capture the effective sub-recipe selections in force in the production tree, not just `newChoices` + top-level outputs; (2) ensure restore resolves stored picks exactly — no silent base-recipe fallback (`Recipe::ofName`) then default-reject (`baseData` ProductionController:29) that reverts a choice; (3) add round-trip restore test: save factory → reload Plan URL through the controller → assert identical recipe selections (single + multi) — must exercise reload, not just the save payload | V57, V32, B39, I.plan-params |
| T111 | .      | ~~Node-splitting *detection*~~ — **no-op (B45), deferred**: provably equivalent to the existing byproduct-boundary heuristic (same SCCs). Reframed to **solve-side byproduct modeling**: include byproduct *output* terms in the balance matrix so a loop member partly supplied by byproduct needs fewer runs (Fix 2); lets byproduct-supplied members stay in the solve instead of being boundary-dropped, without changing detection. Likely folds into T112. Current boundary heuristic retained (correct for all-or-nothing cases incl. caterium) | V68, I.loop-solver, B43, B45 |
| T112 | x      | **DONE (V69).** Sourceless-loop source injection — replace `overrideFavoritesIfNecessary` + generalized `useCompatibleRecipe`: when an unsolvable loop (V58) is sourceless, inject a source — (a) swap a member to its most-efficient loop-free alternate recipe; (b) else auto-import a member. Surface as an informational note (not the circular alarm), user-overridable; never silently swap an explicit user pick (prefer auto-import). Remove `overrideFavoritesIfNecessary`/`useCompatibleRecipe` once landed; rewrite their tests; degenerate Fuel⇄Packaged Fuel resolves via injected Diluted Packaged Fuel (or import) with a clear message; canary rebaseline | V69, I.loop-solver, B44 |
| T96 | .      | Perfect Ratio Factory mode: add "Perfect Ratio" action in Plan Settings (Show.vue); compute `S = LCM(all steps' exact fractional machine-count denominators)` (expose exact fractional demand from backend if displayed count rounded); emit `ScaleOutputs({ratio:S})`; guard oversized S (cap + warn, offer nearest, no apply); tests: post-scale ∀ step num_buildings∈ℤ ∧ clock=100%, mutual-loop plastic/rubber resolves to whole machines, cap-warn path | V56, I.perfect-ratio |
## §B — Bug Log

| id | date | cause | fix |
|----|------|-------|-----|
| B1 | 2026-06-08 | inertia-laravel ^0.6.x caps PHP ~8.3.0; blocked composer update on PHP 8.4 | V9 |
| B2 | 2026-06-08 | 56 of 88 Unit tests were already failing on L9 (MySQL access denied + pre-existing logic bugs) | V2 |
| B3 | 2026-06-08 | L11 upgrade: `assertDeleted` removed; phpunit.xml needed APP_KEY for EncryptionServiceProvider | V2 |
| B4 | 2026-06-08 | ProductionGlobals::$used_byproducts typed as Collection but constructor assigns raw array — missing collect() wrap | V2 |
| B5 | 2026-06-08 | RecipeProductionTest + ProductionStepTest force `database.default=mysql` overriding phpunit.xml sqlite_testing | V2 |
| B6 | 2026-06-08 | DatabaseSeeder only calls FicsmasIngredientSeeder+FicsmasRecipeSeeder; FicsmasRecipeSeeder calls Building::ofName() which returns null (no buildings seeded) | V2 |
| B7 | 2026-06-08 | PHPUnit 11 deprecation: all test files still use @test/@dataProvider doc annotations | V2 |
| B8 | 2026-06-08 | RecipeEnergyTest iron plate/wire — energyStage() variable shadowing masked by DB issue; formula equivalence not established | V15 |
| B9 | 2026-06-08 | .env missing from repo clone → PHP Dotenv warning output → PHPUnit risky tests | V2 |
| B10 | 2026-06-08 | BuildingDetails.php even-rows branch: wrong energy_per_item formula gives ~6× too small value | V15 |
| B11 | 2026-06-08 | raw_materials.php stores MW/(items/min) without ×60; Mk.2 config comment said 12 MW, wiki says 15 MW | V15 |
| B12 | 2026-06-09 | ProductionCalculator.php:48 cache key `'production_calc_ '` has trailing space | T32 |
| B13 | 2026-06-09 | Cross-user cache contamination: favorites=null baked into shared cached production result | V16 |
| B14 | 2026-06-09 | GuestFactories/GuestMultiFactories/GuestFavorites Redis hashes have no TTL | V18 |
| B15 | 2026-06-09 | FlushProductionCache::Redis::select(1) mutates shared phpredis connection for worker lifetime | V17 |
| B16 | 2026-06-09 | FlushProductionCache uses Redis::keys() — O(N) blocking scan | V17 |
| B17 | 2026-06-09 | No cache invalidation on Recipe/Ingredient model save | T35 |
| B18 | 2026-06-09 | BuildingDetails.php even-rows branch updates clock_speed but not energy_per_item/total_energy | V15 |
| B19 | 2026-06-09 | b() helper calls Building::whereName() on every invocation with no caching | T33 |
| B20 | 2026-06-10 | RecipeSeeder Iron Alloy Ingot has v1.0 rates (50/min, 20+20); v1.1 changed to 75/min (40 iron + 10 copper) | V22, T48 |
| B21 | 2026-06-10 | RecipeSeeder Copper Alloy Ingot has v1.0 iron ore rate (25/min); v1.1 changed to 50/min | V23, T49 |
| B22 | 2026-06-10 | Entire Tier 9 quantum endgame chain absent from seeders: no Converter/Quantum Encoder buildings, no SAM processing recipes, no Diamond/Time Crystal/Dark Matter chain, no Quantum Encoder recipes — app was last updated before Satisfactory v1.0 endgame content; all data exists in UpdateOneZero.php helper but never backported | V21, T51–T64 |
| B23 | 2026-06-10 | IngredientSeeder uses legacy names `'SAM Ore'` and `'Green Power Slug'`; canonical names are `'SAM'` and `'Blue Power Slug'` per UpdateOneZero cleanup + Ingredient enum values | V21, T50 |
| B24 | 2026-06-10 | Smokeless Powder added to IngredientSeeder but no Refinery recipe seeded; ProductionTree iterates all processed ingredients and throws ErrorException | T59, T71 |
| B25 | 2026-06-10 | Dissolved Silica added to IngredientSeeder but no base recipe; only appears as byproduct of Quartz Purification; fix: add Refinery entry with Quartz Crystal as byproduct | T71 |
| B26 | 2026-06-10 | energyStage() finds Converter recipes for raw ores (Copper Ore, Iron Ore, etc.) after T68 backport and returns 500 MW Converter cost instead of config mining cost; fix: check isRaw() before recipe lookup | V15, T71 |
| B27 | 2026-06-10 | FuelGenerator fuel map has duplicate key `IONIZED_FUEL` — second entry (15000/5000) silently shadows first; Rocket Fuel absent from generator; packaged liquid fuel variants not listed | V31, T72 |
| B28 | 2026-06-10 | T69 created standalone `FicsoniumFuelRod` generator class — conceptually wrong (it's a fuel, not a building type) and never registered in `PowerPlanner::$options`; `NuclearPowerPlant` already has Ficsonium Fuel Rod as a fuel entry; orphaned class deleted | V30 |
| B29 | 2026-06-10 | `saveMyFactory` PATCH sends only `newChoices` (overridden-from-default recipes) — sub-recipe choices set during the session that happen to match defaults are not persisted; factory reload reverts to default recipe tree — issue #4 | V32, T73 |
| B30 | 2026-06-10 | Multi-output Update: second output added via "Add Output" and fully selected still disappears after fetch — Inertia full-page navigation recreates component; `data()` `outputs` initialized from server `multi` prop which may only include the first output if the fetch dropped the second | V33, T74 |
| B31 | 2026-06-10 | Scale Up Factory scales all outputs proportionally — per-output independent scale not feasible (outputs share ingredient graph); issue #6 closed won't-fix | T75 |
| B32 | 2026-06-12 | Somersloops inert on default recipes: backend keyed `request('somersloops')` lookups via `getDescription()` ('default' fallback) in CalculatesSteps/Getters/ParsesSteps; frontend sends `name\|name`. Test was green because it used backend-shaped key `Iron Ingot\|default` that frontend never emits | V40 |
| B33 | 2026-06-12 | groupedFootprint near-square tile layout ignores belt-required rows — rows = ceil(tiles/ceil(√tiles)) can drop below backend `footprint.rows`, pushing per-row belt load over belt_speed | V45 |
| B34 | 2026-06-12 | T87/V44 made force-even fully inert under blueprint grouping — explicit `even` plan param silently ignored; user intent: even number of blueprint stamps, not even building grid | V46 |
| B35 | 2026-06-12 | V46 stamp bump overwrote belt-derived `$rows` with `ceil(adjusted/buildings_per_row)` (stale divisor, float) — inflated `footprint.rows`; frontend V45 trusts backend rows as belt minimum → grouped diagram forced into extra blueprint rows | V47 |
| B36 | 2026-06-12 | `even` flag never threaded to `groupedFootprint` (prop chain stopped at ProductionSteps) — grouped layout always near-square; even stamp count (V46) still rendered ragged/odd rows, e.g. 8 stamps → 3 rows of 3+3+2 | V49 |
| B37 | 2026-06-12 | V46 parity semantics misread user intent (recorded wrong in B34): bumped 9 stamps (already full 3×3) to 10 → frontend divisor ≥ belt rows forced 5 rows of 2; "force even rows" = full stamp grid (grid-fill, ungrouped analog), not even stamp count | V50 |
| B40 | 2026-06-14 | Circular-dependency handling is ad-hoc + defeats valid loops: Step tree forces a substitute recipe (`useCompatibleRecipe` picks arbitrary `->first()` non-cyclic recipe, CalculatesSteps.php:85) + hard-coded name overrides (`overrideFavoritesIfNecessary`, Setters.php:132 — Recycled Rubber/Plastic, Packaged Fuel); ProductionTree uses a different strategy (cut cycle node to imports, TopSort, ProductionTree.php:72). Recycled Plastic ⇄ Rubber is a valid solvable loop, not an error — forcing a recipe discards the factory the user wants, `->first()` is uncontrolled, and the name-hardcoded branches don't generalize to new game loops. Fix: solve the SCC as a linear system (V58/T98) | V58 |
| B39 | 2026-06-14 | Issue #4 recipe-choice persistence STILL reverts on reload despite T73/V32 marked done. Root: T73 verified only the SEND side — its test asserts the PATCH/POST `choices` payload, never the reload→restore round-trip (`request('choices')` → baseData → page props → re-derived selections), so persistence was never actually proven (cf B32 — green test of wrong shape). Suspected impl gap: `allChosenRecipes` (Show.vue:502) merges only `newChoices` + top-level `form.outputs` recipes — does NOT walk the production tree for effective sub-recipe selections as T73 required; sub-recipe picks absent from newChoices are lost. baseData (ProductionController:29) also default-rejects choices, and `Recipe::ofName` falls back to base recipe for any unmatched description → a stored pick can silently collapse to default on reload | V32, V57, T97 |
| B38 | 2026-06-14 | B31 closed issue #6 "per-output scale not feasible — outputs share ingredient graph" — wrong: sharing the graph ≠ can't independently fill one output to 100%. Per-output fill IS feasible (hold others' net yield, ceil selected product's machine count); mutual loops solvable via integer machine-vector. "Not feasible" conflated "arbitrary free net outputs" with "all machines whole". Reopens T75 → T95 (per-output fill) + T96 (perfect-ratio whole-graph mode) | V55, V56 |
| B45 | 2026-06-15 | T111/V68 node-splitting is a no-op at the DETECTION layer — provably equivalent to the existing byproduct-boundary heuristic: under node-splitting every consumer edge to a byproduct-supplied product routes to the `@byproduct` sink, so the `@recipe` node has zero in-edges and can never join a multi-node SCC; under boundary the product has no node — both yield identical SCCs, no graph distinguishes them. The real V68 value (a loop member only *partially* byproduct-supplied, where the recipe path is still needed) is **quantity-aware → solve-side**, not detection: model byproduct *outputs* in the balance matrix (Fix 2) so demand met by byproduct reduces required runs. Reframe T111 → solve-side byproduct modeling; current boundary heuristic stays (correct for the all-or-nothing cases incl. caterium) | V68, T111, T112 |
| B44 | 2026-06-15 | Forced loop-break override (`overrideFavoritesIfNecessary`) ignored import status: with Fuel=Unpackage Fuel + default Packaged Fuel recipe it forced Packaged Fuel→Diluted Packaged Fuel even when Packaged Fuel was **imported**, populating `production.overrides` → frontend "Circular Dependencies Found - Recipes Adjusted" banner shown despite the import already breaking the loop (and the import silently ignored). Pre-existing (override never checked imports); surfaced reviewing T100b. Fix: `skipOverride()` — skip the forced override when the product is a solved-loop member OR imported | V58 |
| B43 | 2026-06-15 | T100b iterative-convergence solver built + attempted, then reverted: caterium revealed a deeper blocker than B42 — **byproduct-supplied loop members create a spurious unsolvable SCC**. Empty Canister is *supplied as a byproduct* of Unpackage Fuel but also has a recipe (from Plastic), so the selected-recipe graph links Plastic→Fuel→Packaged Fuel→Empty Canister→Plastic into one 5-member SCC {Plastic, Rubber, Fuel, Packaged Fuel, Empty Canister}; the pure input-output linear model (ignoring byproduct supply) finds it singular/non-productive → unsolvable → fallback gives wrong numbers (Plastic=0), a regression vs forced-recipe. Clean loops solve fine (root Plastic⇄Rubber correct, acyclic byte-identical, canary green). Fix needs byproduct-aware loop detection: treat byproduct-supplied products as boundary (cut their recipe edges in the loop graph) or model byproduct supply in the balance matrix. Reverted to T100a; T100b still deferred | V58, T100b |
| B42 | 2026-06-15 | T100b deeper-loop solving has two blockers found during /build, beyond what the task captured: (1) **recursive-tree ⊥ exact-solve impedance** — emitting each loop member once-at-gross (dedupe) destroys the per-consumer demand needed to compute each member's *external* demand, the solver's input; (2) **nested/interacting loops** — caterium selects Recycled Plastic (consumes Fuel, RecipeSeeder:1402) + Unpackage Fuel/Packaged Fuel, so the Plastic⇄Rubber SCC feeds the Fuel⇄Packaged Fuel SCC; a single two-pass undercounts the dependent loop (Fuel demand needs Plastic's solved gross first). Needs ordered (topo over loops) or iterative-to-convergence solving (reuse the byproduct steady-state pattern), and a loop representation that preserves external demand. Deeper loops stay on the forced-recipe fallback (works today, captured by production canary) until a dedicated pass | V58, T100b |
| B41 | 2026-06-15 | V58 stage-2 active-loop detection ("active iff selection covers a candidate cluster's enabling set") never fires for real loops: full-catalog Tarjan SCC (T98) merges every package/unpackage + recycled fluid loop into ONE giant SCC (Plastic, Rubber, Fuel, Packaged Fuel, Heavy Oil Residue, Sulfuric Acid, …) via Unpackage X ⇄ Packaged X back-edges + shared intermediates — its enabling set is huge and never fully selected, so Plastic⇄Rubber is judged inactive and the solver never runs (falls back to forced recipe). Discovered wiring T100a. Fix: detect active loops by Tarjan SCC on the selected-recipe subgraph (one effective recipe per product); catalog = cheap participation gate only | V58, T99, T100a |
